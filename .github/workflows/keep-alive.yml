name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # har 5 daqiqada
  workflow_dispatch:         # qo'lda ham ishlatish mumkin

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Telegram Bot
        run: |
          curl -sf https://debt-tracker-telegram-bot.onrender.com || true
